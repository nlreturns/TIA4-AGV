
/**
 * Write a description of class Ultrasoon here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Ultrasoon{
    
    public Ultrasoon(){
        
    }
    
    /*****
     * // infrared
            int codes = 0;
            int pulse = BoeBot.pulseIn(13, false, 6000);
            if(pulse > 2000){
                
                int lengths[] = new int[12];
                int h[] = new int[12];
                
                for(int i = 0; i < 12; i++)
                    lengths[i] = BoeBot.pulseIn(13, false, 20000);
                
                for(int i = 0; i < 12; i++){
                    if(lengths[i] >= 1000){
                        h[i] =+ 1;
                    }else{
                        h[i] =+ 0;
                    }
                }
                
                for(int i = 0; i < 7; i++)
                    codes += h[i]<<7-i;
                
            }
     * 
     * 
     * 
     */
    
}
